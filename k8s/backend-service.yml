apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: interviewai
  labels:
    app: backend
spec:
  type: ClusterIP       # Internal only — frontend talks to it inside cluster
  selector:
    app: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
